import 'upload_screen_localization.dart';

/// The translations for English (`en`).
class UploadScreenLocalizationsEn extends UploadScreenLocalizations {
  UploadScreenLocalizationsEn([String locale = 'en']) : super(locale);

  @override
  String get pageTitle => 'Upload Product';
}
